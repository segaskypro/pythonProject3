# 🏦 Проект по обработке банковских транзакций

Проект для обработки и анализа банковских транзакций клиента. Решает задачи фильтрации операций по статусу и сортировки по дате для удобного отображения в выписке банковских операций.

## 📋 Содержание
- [Технологии](#технологии)
- [Установка](#установка)
- [Использование](#использование)
- [Структура проекта](#структура-проекта)
- [Разработка](#разработка)
- [Тестирование](#тестирование)
- [Быстрый старт](#быстрый-старт)

## 🛠 Технологии
- **Python 3.14.0** — основной язык разработки
- **pandas 2.3.3** — обработка и анализ данных
- **numpy 2.3.5** — числовые вычисления
- **openpyxl 3.1.5** — работа с Excel файлами
- **pytest 9.0.2** — фреймворк для тестирования
- **flake8 7.3.0** — проверка стиля кода
- **mypy 1.19.0** — проверка статических типов
- **isort 7.0.0** — сортировка импортов
- **black 25.11.0** — форматирование кода

## 🚀 Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/segaskypro/pythonProject3.git
cd pythonProject3
Создайте виртуальное окружение:

bash
python -m venv venv
Активируйте окружение:

Windows (PowerShell):

bash
venv\Scripts\activate
Linux/Mac:

bash
source venv/bin/activate
Установите зависимости:

bash
pip install -r requirements.txt
💻 Использование
Основной скрипт
bash
python main.py
Пример обработки данных:
python
import pandas as pd
from src.transaction_processor import TransactionProcessor

# Загрузка и обработка
processor = TransactionProcessor()
df = processor.load_transactions('data/transactions.csv')

# Статистика
print(f"Всего транзакций: {len(df)}")
print(f"Успешных: {len(df[df['status'] == 'SUCCESS'])}")
Пример использования в коде
python
import pandas as pd
from src.transaction_processor import TransactionProcessor

# Инициализация процессора
processor = TransactionProcessor()

# Загрузка данных
transactions = processor.load_transactions('data/transactions.csv')

# Фильтрация успешных транзакций
successful = processor.filter_by_status(transactions, 'SUCCESS')

# Сортировка по дате (новые сначала)
sorted_tx = processor.sort_by_date(successful, ascending=False)

# Получение статистики
stats = processor.get_statistics(sorted_tx)
print(f"Обработано {stats['total_transactions']} транзакций")
print(f"Общая сумма: {stats['total_amount']:.2f}")
Аргументы командной строки
bash
# Обработка с фильтрацией по статусу
python main.py --status SUCCESS

# Сохранение результатов в Excel
python main.py --output results/transactions.xlsx

# Показать статистику
python main.py --show-stats
📁 Структура проекта
text
pythonProject3/
├── src/                    # Исходный код
│   ├── __init__.py
│   ├── transaction_processor.py
│   └── models.py
├── tests/                  # Тесты
│   ├── __init__.py
│   └── test_processor.py
├── data/                   # Примеры данных
│   └── transactions.csv
├── results/                # Результаты обработки
├── venv/                   # Виртуальное окружение
├── requirements.txt        # Зависимости проекта
├── pyproject.toml         # Конфигурация инструментов
├── README.md              # Документация
└── main.py                # Точка входа
🔧 Разработка
Установка инструментов разработки
bash
pip install -r requirements.txt
Команды для разработки
bash
# Проверка стиля кода
flake8 src/

# Проверка типов
mypy src/

# Сортировка импортов
isort src/

# Форматирование кода
black src/
🧪 Тестирование
bash
# Запуск всех тестов
pytest tests/

# Запуск с подробным выводом
pytest -v

# Запуск с покрытием кода
pytest --cov=src tests/

# Запуск конкретного теста
pytest tests/test_processor.py::TestTransactionProcessor
🚀 Быстрый старт
bash
# Клонировать и запустить
git clone https://github.com/segaskypro/pythonProject3.git
cd pythonProject3
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python main.py
🔗 Ссылки
Репозиторий проекта

Issues & Bug Tracker
